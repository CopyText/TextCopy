<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>net461;netstandard2.0</TargetFrameworks>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(OS)' != 'Windows_NT' ">
    <TargetFramework>netstandard2.0</TargetFramework>
  </PropertyGroup>
  <ItemGroup>
    <Compile Condition="$(TargetFramework)=='net461'" Remove="netstandard\*.cs" />
    <Compile Condition="$(TargetFramework)=='netstandard2.0'" Remove="netdesktop\*.cs" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="ProjectDefaults" Version="1.0.3" PrivateAssets="All" />
  </ItemGroup>
</Project>
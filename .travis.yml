os:
  - linux
  - osx
sudo: required
language: csharp
before_install:
- sudo apt-get install -y xclip
mono: none
dotnet: 2.1.400
script:
 - dotnet restore
 - dotnet build
 - dotnet test Tests/Tests.csproj --no-build